import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PrivacyCash Stellar — Non-Custodial Privacy Protocol",
  description:
    "Private, non-custodial payments on the Stellar network. Powered by Soroban smart contracts, X25519 ECDH cryptography, and PCS governance rewards.",
  keywords: ["Stellar", "Soroban", "stealth address", "privacy", "PCS token", "DeFi"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
